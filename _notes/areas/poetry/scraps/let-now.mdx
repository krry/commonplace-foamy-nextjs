```js
import moment from "moment";

function close(ignorance) {
  let moment = null;
  while (ignorance) {
    ignorance--;
  }
  return 1;
}

function open(kernel) {
  let moment = moment.now;
}
```
